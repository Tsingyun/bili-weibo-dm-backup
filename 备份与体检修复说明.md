# 备份与体检：两个问题的修复说明

> 对应提交 `25ef9a5`。回归：`verify_webui` 205/205 · `verify_extras` 124/124 · 体检两套全绿。

---

## 一、勾了微博 + B站，只跑了微博

### 原因

`scripts/server.mjs` 的 `/api/fetch` 里长这样：

```js
for (const s of chosen) {
  jobs.push(startJob({ ... }));
  break;   // ← 先跑第一个；前端会串行接着发起下一个
}
```

**前端从来没有实现这个"接力"。** 注释描述了一个不存在的行为 —— 只看服务端或只看前端，
都发现不了。于是勾两个平台，第二个永远不会跑。

### 改法：把队列放进任务管理器

`scripts/lib/jobs.mjs` 新增 `queue`：

| 环节 | 行为 |
|---|---|
| `startJob(spec, { queue: 'serial' })` | 任务建出来是 **`queued`** 状态，不启动子进程 |
| `pump(queue)` | 同队列里没有 `running` 的，才轮到队首起跑 |
| 前一个进程 `close` | 自动 `pump` 下一个 |
| `killJob` | 排队中的任务直接从队列摘掉（状态 `cancelled`） |

为什么必须**串行**而不是并行：两个平台同时抓会抢**同一个浏览器调试端口**
（后起的连不上 CDP 直接失败）和同一块写盘，日志也会糊在一起分不清。

### 界面上的变化

* 点「开始备份」→ 两个任务都建出来，第一个跑，第二个显示「排队中…（等前面那个平台跑完就自动开始）」
* 第一个结束后自动接上第二个，日志连着往下打（会插一条 `════ 备份 · B站 ════` 分隔）
* 点「停止」→ 当前任务 + **后面排队的**一起取消（不然杀了当前那个，下一个会自己跑起来）

## 二、B站体检报「ocr.json 有 1 个键指向不存在的图片」

```
· ocr.json 有 1 个键指向不存在的图片，例如 card_u4oc9m.webp
```

### 原因

图片压缩把 `card_u4oc9m.webp` 转码成了 `.jpg`，**新的键写进去了、旧扩展名的那个没删** ——
同一张图在索引里留了两把键，`.webp` 那把成了永远指不到图的死键。

死键没有任何用处，但会让体检永远报「错误 1 项」，看起来像备份坏了。

### 改法：`doctor --fix` 新增「清孤儿键」

```
node scripts/doctor.mjs --fix --dry-run   # 先看会改什么
node scripts/doctor.mjs --fix             # 真修
```

执行顺序：`.bak-<时间戳>` 备份 → 删死键 → 修正 `_meta.count` → **原子写回**（先 .tmp 再改名）
→ 同步重新生成 `ocr.js` 包装（查看页读的是 js，只清 json 等于没清）。

这次已对你的真实数据执行过：B站 OCR 键 **410 → 409**，`_meta.count` 同步，两套体检现在全绿。

> 附带修掉一个体验问题：以前 `--fix` 修完仍然打印「合计：错误 1」、退出码还是 1
> （因为已修好的项还留在报告里）—— 定时自动更新会一直判 NG，用户看到的是"修了但没修好"。
> 现在修好的项会从报告里划掉再重算，退出码归 0。

## 三、顺手修掉的两处"测试自己假红"

| 项 | 原因 | 处理 |
|---|---|---|
| `B站自动回复条数 = 488` 断言失败 | 你 02:31 新抓了 33 条，auto 从 488 变 492 | 断言改成「**不低于**历史锚点 488」（增量抓取只会让它变多） |
| `build_fts --check` 对不上（7925/7942、2912/2945） | 数据比搜索索引新，不是 bug | 重建索引：`node scripts/build_fts.mjs` → 10887 条 |

## 四、怎么验证

```
node scripts/_explore/verify_webui.mjs     # 205/205（含 7.9 多平台串行）
node scripts/_explore/verify_extras.mjs    # 124/124（含 doctor --fix 清孤儿键）
node scripts/doctor.mjs                    # 两套体检：全部正常
```

界面验证：打开 WebUI 第 4 步（刷新一下页面即可，静态文件是每次读盘的），
两个平台都勾上 → 开始备份 → 应该看到第二个任务显示「排队中」，第一个跑完自动接上。
