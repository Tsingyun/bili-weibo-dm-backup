# 私信备份（微博 + B站）

把**微博私信**和 **B站私信**的**全部聊天记录 + 图片**
导出到本地，配一个离线可看的网页，**两个来源在同一个页面里切换**，并且支持**随时一键增量更新**。

聊天对象在 `sessions.json` 里配置（微博 / B站 各一个，改了就换人）。

| 来源 | 当前备份 | 更新用 | 在查看页顶部选 |
|---|---|---|---|
| **微博** | 7,925 条消息 · 1,018 张图片，2024-01-31 → 2026-09-15 | `命令/备份与更新/更新备份.cmd` | 「微博」 |
| **B站** | 2,912 条消息 · 427 张图片，2022-09-05 → 2026-09-15 | `命令/备份与更新/更新B站备份.cmd` | 「B站」 |

会话清单在 `sessions.json`，改完跑一次 `命令/配置与定时/生成会话清单.cmd`（多会话支持见 **第 9 节**）。
图片已统一压成 WebP（**原图已删**，见 **第 10 节**）：微博 2.0 GB → 174 MB，B站 445 MB → 58 MB。

---

## 一、怎么用

> **页面顶部有一个「微博 / B站」切换按钮**，两个来源共用一个查看页，功能完全一致
> （搜索、图片文字、互动统计、展开上下文……都跟着切换）。按钮上的数字是那个来源的消息总数；
> 上次看的是哪个来源会被记住，下次打开还是它。数据还没生成时按钮也能点，
> 进去会告诉你该跑哪个更新程序。

### 1. 查看备份
双击 **`查看备份.html`** 即可（无需联网、无需服务器）。

页面是**三栏布局**：左栏是常驻状态栏（滚动时固定不动），中间是聊天记录，宽屏（≥1340px）时右侧再多一条月份时间轴。

| 功能 | 说明 |
|---|---|
| 搜索 | 左栏搜索框，按 `/` 快速聚焦；回车或 ↓ 跳下一个匹配、Shift+回车或 ↑ 跳上一个，命中处高亮，旁边显示「第 N / M 条」 |
| 筛选 | 左栏：全部 / TA 发的 / 我发的 / 含图片 |
| 隐藏自动回复 | 左栏 **🚫 自动回复**，一键隐藏 1352 条模板回复「（对方设置的自动回复原文）」，再点恢复；开关状态会被记住。开启后**列表、搜索结果、展开的上下文里都不会出现**它（见第 3 节） |
| **隐藏某一条消息** | 在任意消息上**右键**（手机**长按 0.5 秒**）→ **🙈 隐藏这条消息**。隐藏后列表与搜索里都不再出现；只在展开上下文时留一条虚线占位条，点一下即可恢复（见第 15 节） |
| 跳日期 | 左栏选择日期，直接定位到那天（该日期按 **05:00 分界**，见下一节） |
| 月份时间轴 | 右栏（宽屏）按月导航，显示每月条数并高亮当前所在月份，点一下直接跳过去；搜索/筛选后时间轴会跟着变 |
| 看大图 | 点任意图片 → 灯箱，支持 ← → 翻页、下载原图 |
| 分享微博卡片 | 默认收成「作者 + 两行摘要 + 小图」，点 **展开详情** 看全文与配图，再点 **收起** 收回 |
| 文字表情 | `[委屈]`、`[泪]`、`/ee808d.png` 这类会自动还原成微博官方表情图，并随文字大小缩放 |
| 主题 | 左栏 🌗 切换浅色 / 深色 |
| 回到最早/最新 | 左栏底部两个按钮（窄屏时改为右下角悬浮按钮） |
| **互动统计** | 左栏 **📊 互动统计** → 按天统计「我发出」与「对方回复」的条数，并用折线图展示每日趋势（见下一节） |
| **展开上下文** | 搜索结果里每条命中消息下方都有 **↕ 展开上下文**，点开就能看到它前后的真实对话（见第 3 节） |

窗口宽度不够时布局会自动降级：≥1340px 显示三栏；980–1340px 只保留左栏；<980px 左栏变为顶部横排（滚动时吸顶），保证手机上也能正常用。

### 2. 按天互动统计（折线图）
左栏点 **📊 互动统计** 打开面板，统计**每天「我发了几条」和「对方回复了几条」**。

**统计口径**（面板底部的「统计口径与数据来源」里也写着，数字实时从本地备份算出）：

| 项目 | 规则 |
|---|---|
| 数据来源 | 本地 `data/messages.js`（7925 条），**纯本地计算，不联网** |
| **按「每天 05:00」分界** | 一天 = **当天 05:00 → 次日 05:00**。一条消息归属**它前面最近的那个 05:00** 那一天，所以 **00:00~05:00 的消息算前一天**（不按自然日 0 点切） |
| 时间轴 | 按日**连续补齐**，没有消息的日子记 0（共 306 天），所以折线上的时间间隔是真实的 |
| 谁发的 | 用接口返回的 `from` 字段判定：`me` = 我，`peer` = 对方。不靠昵称或文案猜测 |
| **不计入** | ① 自动回复（对方设置的固定文案）**1352 条**（对方 1346 · 我 6）<br>② 送礼物 / 助威系统通知 **5 条**（对方 4 · 我 1，含 1 条红包提示）<br>③ 撤回提示「你 / 对方撤回了一条消息」**34 条**（我 33 · 对方 1） |
| **计入** | 分享微博卡片、分享图片 / 视频 / 语音、动图表情、链接、文字等**全部真实消息**；被撤回但原文仍在的消息也计入（它确实发出过） |

> **为什么要按 05:00 分界**：聊天常常一聊就到深夜。按自然日 0 点切，一段「23:40 聊到 01:20」的
> 对话会被劈成两天；这份备份里落在 00:00~05:00 的消息有 **2675 条，占全部 33.8%**，劈开之后
> 「今天聊了几句」「哪天有来有回」都会失真。改成 05:00 分界后，凌晨那一段算进**前一天**，
> 一整晚的对话就落在同一天了。

> 易踩的坑：日常聊天里**提到**「礼物」两个字的消息有 11 条（如「舰长的礼物会有音效吗」），
> 属于真实对话，**计入**统计 —— 被排除的只是微博的助威系统通知，不是关键词一刀切。

> **同一条分界线贯穿全站**：聊天里的**日期分隔条**、右栏**月份时间轴**、「跳到日期」、
> 高级筛选的**日期区间**、折线图的**连续日轴**、日历热力图，以及「一周里哪天最活跃」，
> 全部按 05:00 算 —— 不会出现「分隔条写 6月10日、筛选却按 6月11日 取」这种自相矛盾。

**当前结果**（区间 2024-01-31 ~ 2026-09-15，共 959 天，其中 306 天无消息）：
我 **6415** 条（日均 6.7）· 对方 **119** 条（日均 0.1）·
有来有回 **90 / 959** 天 · 我的单日峰值 **53** 条（2025-05-23）。

**图上操作**：

| 控件 | 说明 |
|---|---|
| 时间范围 | 近 30 天 / 近 90 天 / 近 1 年 / 全部（默认全部） |
| **7 日均线** | 默认开。细线是每天原始值，粗实线是 7 日移动平均，用来抹平单日波动看趋势 |
| **独立量程** | 默认开。左右各一套刻度（左轴=我、右轴=对方）。两方量级相差数十倍（6415 : 119），同轴时对方那条会贴底看不见；想比较真实量级就关掉它，提示条会说明当前模式 |
| 悬停 | 显示当天日期、星期与两边的条数 |
| 点击 | 点图上任意一天 → 直接跳到那天的聊天记录（若该天被搜索/筛选滤掉，会先清空条件再跳） |

> 关于「按天」的一个提示：全部范围是 959 天，在 1000px 宽的图里每天不到 1.2 像素，
> 折线会很密（像密度带）。想看每天的细节，切到「近 90 天」最清楚（约 12 像素/天）。

### 3. 展开上下文（看命中的前后对话）

搜索出来的结果是一条条孤立的，看不到它前后的对话。每条**命中消息的下方**都有一个
**↕ 展开上下文** 按钮，点开就把这条消息在**完整会话**里的前后消息一并显示出来。

**触发方式**

| 出现位置 | 控件 | 作用 |
|---|---|---|
| 命中消息下方 | **↕ 展开上下文**　前后各 3 条 | 首次展开 |
| 命中消息下方（已展开后） | 已展开：更早 N 条 · 更晚 N 条 ＋ **↑ 更早 +5** ／ **↓ 更晚 +5** ／ **收起** | 就近单侧加量、一键收起 |
| 上下文区块**顶部** | **↑ 加载更早的上下文 +5 条** | 按钮就在新内容将要出现的位置 |
| 上下文区块**底部** | **↓ 加载更晚的上下文 +5 条** | 同上，加载完不用回头找按钮 |

> 这些按钮**只在搜索时出现**。没有搜索词时（比如只按「全部」或「TA 发的」筛选），
> 页面跟以前完全一样，不会多出任何控件。

**每次展示多少条 / 怎么加载更多**

| 项目 | 规则 |
|---|---|
| 首次展开 | 前后各 **3 条**（靠近会话首尾时自动少到只剩可用的条数） |
| 每次「加载更多」 | 该侧 **+5 条**，可反复点，一直到会话头 / 尾 |
| 两侧独立 | 更早与更晚各算各的，互不影响 |
| 加载位置 | 上端条管更早、下端条管更晚，**点完视线不用跳** |
| 位置保持 | 插入内容后，那条命中消息会被**钉在原地**，不会因为上方变高而跑出视野 |
| 收起 | 点命中消息下方的 **收起**，上下文消失，恢复成原来的搜索结果 |
| 自动重置 | 换了搜索词或筛选条件，已展开的上下文**自动清空**（避免张冠李戴） |

**上下文区域里怎么高亮当前命中的那条**

| 样式 | 含义 |
|---|---|
| 🟠 **琥珀色方框 + 「命中」角标** | 你展开上下文的那一条（全页同时只会有一个角标） |
| 细描边 + 小字「· 命中」 | 落在别人上下文里的**另一条**搜索结果，用来和上下文区分 |
| 整块淡化 + 一条竖线 | 上下文消息。自己发的竖线在**右**，对方发的在**左**，一眼看出哪段是"顺带看到的" |
| 侧栏数字不变 | 「第 N / M 条」的 M 不会因为展开而变 —— 上下文只是"看"，**不计入命中** |

**上下文是从哪来的**

取自**完整会话**（`data/messages.js` 里的全部 7925 条），**不受当前搜索词和筛选条件影响**。
所以你看到的是真实对话流，包括被搜索条件筛掉的那些消息 —— 这正是它的用处。

**两个例外**（都是"你已经明确说过不想看"的内容，再显示出来就自相矛盾了）：

| 情况 | 上下文里的表现 |
|---|---|
| 开着 **🚫 自动回复** | 自动回复**整条跳过**，不占位置也不留占位（左右两侧的「更早 N / 更晚 N 条」说的是**展开范围**，不是实际画出来的行数） |
| 用右键**单独隐藏**过的消息 | 留一条虚线占位条「🙈 这条消息已隐藏　恢复」，**正文与图片都不渲染**；点占位条即可恢复 |

> 这个"跳过"是 2026-09-16 修的 bug：以前自动回复虽然不显示正文，却会在上下文里
> 留下一个只剩 `[文字]` 的空气泡，看起来像是消息丢了或页面坏了。

**没有更多上下文 / 消息在会话首尾时怎么显示**

| 情况 | 显示 |
|---|---|
| 已经到会话**最开头**（上面都加载完了） | 上端分隔条变成 `↑ 这已经是你们最早的对话`，**不再出加载按钮**；命中条下方的「↑ 更早」一并隐藏 |
| 已经到会话**最末尾** | 下端分隔条变成 `↓ 这已经是最后一条消息`，同样不出加载按钮 |
| 命中消息本身就是**第一条 / 最后一条** | 只展开存在的另一侧，数字如实写成「更早 0 条」或「更晚 0 条」 |
| 上下文和**另一条命中**重叠 | 同一条消息只画一次、分隔条也只留一条；点一下**同时为两边加载**，不重复也不漏 |
| 上下文里出现**日期分界线** | 跨天时照常插入「XXXX年X月X日 星期X」分隔，跟正常聊天一样 |

> 例：搜索「晚上吃」命中的那条「醒了 晚上吃什么呀」，展开后前后就是你俩当时的真实对话
> （「我要睡觉了」→「晚上再来找你玩」→ *命中* →「给你一拳」）。

---

### 4. 更新备份（拉取新增聊天）
双击 **`命令/备份与更新/更新备份.cmd`**，等它跑完就行（增量，通常几秒到几十秒）。

它会自动：
1. 启动专用浏览器实例（已登录状态，**不用重新扫码**）；
2. 只拉取**新增**的私信；
3. 下载新出现的图片；
4. 刷新查看页面的数据；
5. 补齐新出现的官方表情图（只下缺的，通常几秒）；
6. 识别新增图片里的文字（**只有装了 OCR 环境才会跑**，见下面第 7 节；没装就自动跳过）。

### 5. 全量重建（一般用不到）
双击 **`命令/备份与更新/重建备份.cmd`**。它会忽略本地记录、按当前规则把所有消息重抓一遍，
用于「改了整理规则后想重新生成全部数据」。原数据会自动备份到
`data/raw/messages.bak.json`，不会丢。

### 6. 如果提示需要登录
微博 Cookie 会过期（通常几个月）。届时脚本会自动打开专用浏览器窗口，
**在里面扫码登录一次**，再运行一次 `命令/备份与更新/更新备份.cmd` 即可。
登录态保存在 `.edge-profile` 里，之后又能长期自动更新。

> 手动运行（等价于上面的双击）：
> ```powershell
> powershell -ExecutionPolicy Bypass -File scripts\update.ps1
> ```
> 可选参数：`-Full` 强制完整重拉（补齐历史空洞）｜`-NoImg` 只更新文字｜`-Rebuild` 全量重建。

### 7. 搜索图片里的内容（可选功能）

搜索框除了搜聊天文字，**也能搜到图片里的内容**。分两条互相独立的路径：

| 路径 | 能搜到什么 | 要不要装东西 | 图片会外传吗 |
|---|---|---|---|
| **图内文字**（本地 OCR） | 截图里的对话、视频封面标题、设定图标注 | 要装一次本地环境（约 330 MB） | **不会**，全程在本机识别 |
| **图片内容描述**（云端模型） | 照片拍的是什么：风景／人像／舞台／宠物／插画 | 不用装，填一个免费 API Key | 会，但**只上传没有文字的图片**；有文字的截图永不上传 |

两者结果都汇进同一个搜索框。命中的图片会描边：**搜到图里的字是蓝框，搜到内容描述是粉框**。

#### 7.1 图内文字（本地 OCR）

1. 双击 **`命令/图片与OCR/安装OCR环境.cmd`**，等它装完（约 330 MB）；
2. 装完它会问你是否马上开始识别（845 张，实测约 11 分钟；中途可以 Ctrl+C，进度会保留，下次接着跑）；
3. 之后在搜索框直接搜图里的词就行。以后每次 `命令/备份与更新/更新备份.cmd` 会自动识别新增的那几张。

#### 7.2 图片内容描述（云端，需要 API Key）

没有文字的纯照片（风景、人像、舞台照）OCR 认不出来，所以交给视觉模型写一段
可搜索的描述，例如「星空、沙漠岩石、波浪状砂岩、夜景、橙黄色调…」。

1. 到 [智谱开放平台](https://bigmodel.cn) 注册，创建 API Key
   （下面两个都是官方**永久免费**模型，不花钱）；
2. 把 Key 粘进 `data/glm_key.txt`（一行，不要加空格或引号）；
3. 双击 `命令/备份与更新/更新备份.cmd` 即可 —— 它会自动为新增的无文字图片生成描述。
   想手动跑全量：`.ocr-env\Scripts\python.exe scripts\image_vlm.py`

**关于免费额度与限流**：脚本内置一条**免费模型降级链**，默认
`glm-4.6v-flash → glm-4v-flash`。请求遇到 429「访问量过大」时会**自动换到链上另一个
免费模型**继续跑，而不是干等；每张图都从链首开始，所以主模型空闲时会自动切回质量更好的那个。

> 实测对比（同样 100 多张图）：只用单一模型时，高峰期 1 小时 45 分只成功 82/126；
> 加上降级链后，剩余 44 张 **5 分 36 秒全部完成，零失败**。
> 想换模型或改链序：`--model glm-4.6v-flash,glm-4v-flash`（逗号分隔，按顺序降级）。
> 按官方定价页，永久免费的视觉模型只有三个：
> `glm-4.6v-flash` / `glm-4v-flash` / `glm-4.1v-thinking-flash`，
> 其余（GLM-4.6V、GLM-4.5V、GLM-4V-Plus…）都是**付费**的，别填错。

**描述会被自动清洗后再入库**：模型有时不守规矩，脚本会统一修掉——
去掉 `<think>` 思考过程、markdown 记号（`**关键词：**`、`- 列表`）与自加的小标题；
关键词去重并截到 30 个（有个模型会复读成「美味佳肴、美味食物、美味佳肴…」一路写到上限，
单条最长冲到 1,912 字、300 个词里只有 30 个是唯一的）；总长限制 200 字。
中文引号等内容原样保留，不会被误删（回归测试见 `scripts/_explore/verify_vlm_clean.py`）。

> 中途中断也没关系，重跑会跳过已经生成好的。

#### 7.3 不想要了怎么彻底卸载

双击 **`命令/图片与OCR/卸载OCR环境.cmd`**，它会**逐项问你要不要删**，不碰其它任何东西：

| 问什么 | 删掉什么 | 删了有什么影响 |
|---|---|---|
| 运行环境 [1] | `.ocr-env` 整个文件夹（约 330 MB） | 不能再识别新图片；已识别好的照常能搜 |
| 图片索引 [2] | `data/ocr.*` 与 `data/vlm.*`（几百 KB） | 搜索框不再匹配图片内容 |
| API Key [3] | `data/glm_key.txt` | 想再用图片描述时需重新填 |

**关键点：索引是纯数据，跟运行环境无关。** 只删 [1] 的话，
`查看备份.html` 照常使用、**图片搜索依然有效**，只是没法识别以后的新图。

| 项目 | 说明 |
|---|---|
| 装在哪 | 只有一个文件夹 `.ocr-env`（Python 环境 + 模型，模型随包分发，运行时不额外下载） |
| 还会写哪 | 只有 `data/ocr.*`、`data/vlm.*`、`data/glm_key.txt` |
| 用户目录 | **零写入** —— 已验证不产生 `~/.rapidocr`、AppData 缓存等任何残留 |
| 隐私 | OCR 100% 在本机进行；云端描述只发「OCR 判定为无文字」的照片，含文字的截图不上传 |
| 不想装 | 不装也能正常用，只是搜不到图片内容；更新流程会自动跳过这一步 |

### 8. 备份 B站私信

B站私信是**独立的第二套备份**，操作方式和微博那套一模一样，只是用另一组文件、另一个更新入口。

#### 8.1 第一次用：先登录 B站

1. 双击 **`命令/备份与更新/更新B站备份.cmd`**；
2. 它会自动打开专用浏览器窗口，并停在 **B站私信页**（就是和对方的对话窗口）；
3. **在这个窗口里扫码登录 B站**（只需要一次，之后长期有效）；
4. 登录成功后，**再双击一次 `命令/备份与更新/更新B站备份.cmd`** —— 这次就会正式开始拉取聊天记录。

#### 8.2 以后更新

双击 **`命令/备份与更新/更新B站备份.cmd`** 就行了（增量）。流程和微博完全一致：
拉取新增私信 → 下载图片 → 刷新数据 → 同步 B站表情图 → 识别新图里的文字 → 生成图片描述。

| 文件 | 作用 |
|---|---|
| `命令/备份与更新/更新B站备份.cmd` | 一键增量更新 B站私信 |
| `命令/备份与更新/重建B站备份.cmd` | 全量重建（只有改了整理规则才需要）；原数据自动备份到 `bili/raw/messages.bak.json` |

> 手动运行（等价于双击）：
> ```powershell
> powershell -ExecutionPolicy Bypass -File scripts\bili.ps1
> ```
> 可选参数：`-Full` 全量重拉（补历史空洞）｜`-NoImg` 只更新文字｜`-Rebuild` 全量重建｜
> `-Probe` 只抓一页并输出「消息类型审计报告」（排错用，不改动正式数据）。

#### 8.3 B站和微博有哪些不一样

| 项目 | 微博 | B站 |
|---|---|---|
| 数据目录 | `data/` | `bili/` |
| 登录凭据 | `data/cookie_header.txt` | `bili/cookie_header.txt` |
| 登录用的浏览器 | 共用同一个专用浏览器（`.edge-profile`） | 同上；两个网站各存各的登录态，互不影响 |
| 自动回复识别 | 精确匹配模板文案「（对方设置的自动回复原文）」 | 按接口字段**结构化判定**：`msg_source` 为 8~11 / 17，或 `msg_type` 为 16（更可靠，不用猜文案） |
| 自动回复一键隐藏 | 查看页左侧「🚫 自动回复」按钮 | **同一个按钮**，两边各自记忆开关状态，互不串 |
| 图片文字检索 / 图片描述 | 搜索框直接搜图里的文字或图片内容 | **完全一致**，索引各用各的（`data/ocr.js`·`data/vlm.js` ↔ `bili/ocr.js`·`bili/vlm.js`） |
| 卡片 | 分享的微博 | 分享的视频 / 专栏 / 动态 / 直播、视频推送、UP主赠言、官方通知 |
| 表情 | `[委屈]`、`[泪]` 这类文字表情 | `[tv_doge]` 这类 B站表情 |

> B站的时间戳里带着 19 位大整数（`msg_key`、`msg_seqno`），脚本会先把它们转成字符串再解析，
> 避免 JavaScript 浮点精度丢失导致消息去重失效。
>
> B站私信接口对同一会话**最多只返回 2000 条一页**，脚本按 500 条翻页并每 20 页断点落盘，
> 中断后重跑会接着来。

#### 8.4 不想扫码？直接贴 Cookie（可选）

脚本取登录凭据的顺序是：**先读 `bili/cookie_header.txt` → 再尝试从专用浏览器刷新**。
所以只要把浏览器里的 B站 Cookie 原样粘贴进 `bili/cookie_header.txt`（一行，就是
`name=value; name=value; …` 那种格式），就能**完全跳过扫码**，直接跑更新。

必须带的两个字段是 `SESSDATA` 和 `bili_jct`；`DedeUserID` 也建议带上。
粘贴后直接双击 `命令/备份与更新/更新B站备份.cmd` 即可。想确认凭据对不对，先跑一次探查：

```powershell
powershell -ExecutionPolicy Bypass -File scripts\bili.ps1 -Probe
```

会打印 `登录账号：xxx (<mid>)` 和聊天对象，能对上就说明 Cookie 有效。

> ⚠️ `cookie_header.txt` 等同于账号密码，**别发给别人、别传到网上**。
> 如果哪天更新报「登录态已失效」（退出码 3），重新粘贴一份新的 Cookie 就行。
> 这个文件不会被任何脚本自动清理，想失效随时手动删掉即可。

#### 8.5 首次备份实测规模（2026-09-15）

| 项目 | 数值 |
|---|---|
| 消息总数 | 2,912 条 |
| 时间范围 | 2022-09-05 → 2026-09-14（约 4 年） |
| 图片 | 425 张（另含双方头像 2 张） |
| 表情图 | 1,177 个 |
| 图片文字索引（OCR） | 401 张，其中 313 张识别到文字 |
| 图片内容描述（VLM） | 88 张无文字图片（另有 1 张 GIF 动图，按设计不送云端） |

- 消息类型分布：文字 2,459 · 图片 283 · 卡片 134 · 表情 23 · 撤回 13。
- 图片描述的**目标集**是「非头像/表情前缀、非 GIF、且 OCR 判定没有文字」的照片，
  当前 88/88 全部完成，与微博那套口径一致。
- 有 **1 张图片**（2025-10-11 分享的相簿封面）在抓取时远端就已 404，本地拿不到。
  查看页遇到这种图会显示「原图已失效」的虚线占位，不会留一个破图图标。
- 聊天对象昵称/头像取自 `cards` 接口。**该接口返回的 `data` 是数组**（`[{mid,name,face}]`），
  早期代码按对象取键 `data[mid]` 一直取不到，导致昵称走兜底、头像为空；已修复并加了单测
  `scripts/_explore/verify_cards.mjs`。

#### 8.6 自动回复与图片索引，在页面上怎么用

B站视图和微博视图**共用同一套交互**，切到 B站 后这些都在：

| 想做什么 | 怎么做 |
|---|---|
| 隐藏对方的自动回复 | 左侧「🚫 自动回复」按钮一点即可（本图数据是 **488 条**）；再点恢复。开关状态按数据源各自记忆 |
| 搜图片里的文字 | 直接在搜索框输入图里的字（如截图上的对话） |
| 搜图片内容 | 输入画面描述（如「白发红瞳」）—— 每张照片下方也会直接把描述显示出来 |
| 只看图片 | 左侧筛选选「含图片」 |

> 自动回复的判定**不靠文案**：B站接口的 `msg_source` 字段（8 关注后 / 9 收到消息 / 10 关键词 /
> 11 大航海 / 17 互关）直接标明它是不是自动回复，所以文案改了也不会漏。
> 唯一的例外是**用户自己发的截图**——截图 OCR 里可能恰好含自动回复的文字，
> 那不是自动回复消息，隐藏时不会误伤。

#### 8.7 分享卡片的显示（2026-09-15 修复）

分享的视频卡片里，**「🔗 打开原链接」**和卡片标题现在都是可读的深色文字。

- 之前浅色主题下它们是**白字**，而卡片所在的气泡因为「有封面图、没有文字」是**透明底**的，
  于是白字直接落在浅色页面上，实测对比度只有 **1.06:1**，基本看不见。现修好后为 **5.19:1**（深色主题 8.02:1）。
- 长标题与卡片正文重复的问题也一并修了：分享视频的标题和正文是同一串字（实测 130 条卡片里 90 条完全相同），
  原来会上下重复显示两遍，现在只显示一次。
- 这两点都有回归断言守着（`scripts/_explore/shot_bili_real.mjs` 第 9 节，浅色/深色各测一遍），
  并且做过**反向验证**：把旧写法改回去，断言会立刻报红。

---

### 9. 多会话（想再备份一个聊天对象）

所有会话都写在项目根的 **`sessions.json`** 里，它是**唯一数据源** —— 查看页、更新脚本、
图片索引脚本读的是同一份清单，不会出现「页面认识这个会话、脚本不认识」的错位。

1. 在 `sessions.json` 的 `sessions` 数组里照着现成的 `weibo` / `bili` 抄一份，改 `key` / `label` / `dir`。
2. 双击 **`命令/配置与定时/生成会话清单.cmd`** —— 它重新生成 `sessions.js`（页面读它），并顺手补齐新会话的数据目录骨架。
3. 打开查看页，顶部就会多出这个会话的按钮。

只有 `weibo` 与 `bili` 是**内置会话**（查看备份.html 里写死了它们的 `<script>` 标签，
所以 `globals` 必须是历史短名 `DM_DATA` / `DM_DATA_B`，**不要改**）。
新增的会话由页面在解析期动态插入 `<script>` 标签加载，功能完全一致。

| 给脚本指定会话 | 写法 |
|---|---|
| 更新脚本 | `node scripts/update.mjs --session <key>` |
| 图片文字识别 / 图片描述 | `python scripts/image_ocr.py --set <key>`（`image_vlm.py` 同理） |
| 导出 JSONL / 表情包 | `node scripts/export_jsonl.mjs --session <key>`（`export_faces.mjs` 同理） |

> `sessions.json` 里 `_meta.schema` 那一大段就是字段说明（`platform`、`skip`、`autoReply`、
> `empty`、`globals`、`builtin` 各自什么含义），照着填即可。

---

### 10. 图片压缩（WebP）

图片是备份里唯一的大头。压缩工具会把 `jpg/png/bmp` 统一转成 **WebP**，
并**同步改掉三处索引里的文件名键**，最后删掉原图。

**双击 `命令/图片与OCR/压缩图片.cmd`** 即可（会先问一次 Y/N）。默认参数 `quality=88`、长边上限 `2048`。

| 项目 | 数值 |
|---|---|
| 微博（`data/images/`） | 约 2.0 GB → 约 300 MB |
| B站（`bili/images/`） | 约 445 MB → 约 60 MB |

**刻意不动两类东西**：`*.gif`（动图转 WebP 有丢帧风险）和 `avatar_*.jpg`
（更新脚本里头像路径是硬编码 `.jpg` 的，动了会让头像失联）。

工具是**五个阶段、顺序不可调换**的，任何一步出错就停住、**一张原图都不删**：

```
扫描 → 转码（先写 .tmp 再改名，不留半个文件） → 迁移索引 → 校验 → 才删原图
```

* 能**反复重跑**：已经转好的会识别成「沿用」，索引迁移是幂等的。
* 收益不足的图（压完不比原来小 0.5% 以上）会**保留原图**，不做「压完更大」的蠢事。
* 校验内容：消息条数不变 · 每条图片引用与每个索引键都指向磁盘上真实存在的文件 ·
  `messages.js` 与 `messages.json` 的图片路径完全一致。

> ⚠ **这一步会删掉上千个文件，必须在普通命令行窗口里跑**（双击就是普通窗口）。
> 在 WorkBuddy 这类受控环境里，它的「批量删除保护」（默认上限 50 个/轮）会在删除阶段
> 直接中止进程 —— 此时**索引已迁移、原图还在**，属于可恢复状态，重跑一次即可。
> 想改压缩参数（更省 / 更清晰），在命令行里跑：
> `.ocr-env\Scripts\python.exe scripts\compress_images.py --set all --quality 82 --max-edge 1600`

---

### 11. 定时自动更新（默认每周一早上 8:00）

双击 **`命令/配置与定时/注册定时更新.cmd`** 一次即可（**默认每周一早上 8:00**）。
要改星期几 / 改时间就带参数：`命令/配置与定时/注册定时更新.cmd -DaysOfWeek Monday,Thursday -At 09:30`；
想改成每天跑，加一个 `-Daily`。取消用 **`命令/配置与定时/取消定时更新.cmd`**。

> 触发时间、数据来源、同步范围、存储位置、失败处理的完整说明（含实测回读的任务参数）
> 见 **《定时自动更新说明.md》**。

用的是 **Windows 计划任务**，注册在**当前登录用户**下（`-LogonType Interactive`）——
抓取需要桌面会话，所以**必须在登录状态下才会跑**，不会在开机前或注销后偷偷执行。

每次会依次做：增量更新（微博 + B站）→ 图内文字（OCR）→ 图片描述（VLM，免费模型）
→ 索引快照 → 体检，日志写在 `logs/auto_<时间戳>.log`（只保留最近 60 份）。

| 想改频率 / 少跑一些 | 参数 |
|---|---|
| 改成每天跑 | `命令/配置与定时/注册定时更新.cmd -Daily -At 21:30` |
| 改成每周一、周四 | `命令/配置与定时/注册定时更新.cmd -DaysOfWeek Monday,Thursday` |
| 只更新微博 | `命令/配置与定时/注册定时更新.cmd -Sessions weibo` |
| 只更新数据、跳过所有图片索引 | `命令/配置与定时/注册定时更新.cmd -SkipIndex -SkipVlm` |
| 顺带压缩新图片 | `命令/配置与定时/注册定时更新.cmd -Compress` |

想先看效果，可以双击 `命令/配置与定时/注册定时更新.cmd -RunNow`（立刻跑一次，不等到点）。
**全程不联网调云端服务**，图片描述那一步也只用智谱官方标注「永久免费」的模型。

---

### 12. 导出 JSONL（喂给别的工具）

双击 **`命令/查看与导出/导出JSONL.cmd`**，会把聊天记录导成一行一条的 JSONL。默认导出**全部会话**到
`exports/`，微博 7,925 + B站 2,912 = **10,837 行**（约 4.4 MB）。

每条包含：`session / i / id / ts / date / time / utc / from / sender / type /
media_type / msg_source / recalled / kind / text / images / ocr / vlm / card / links`。
`kind` 用的是页面同一套分类口径；`images` 里带 OCR 文字与图片描述。

| 常用参数 | 作用 |
|---|---|
| `--session bili` | 只导某一个会话 |
| `--since 2026-01-01 --until 2026-06-30` | 按时间窗筛 |
| `--mask` | 打码手机号 / 邮箱 / 身份证 / 银行卡 / 账号 / 收货地址 |
| `--no-ocr --no-vlm --no-images --no-card` | 瘦身，只要纯文本 |
| `--stdout` | 直接打到屏幕，不落文件 |

> `--since` / `--until` 用的是**和第 2 节同一条分界线**：`--since 2026-01-01` 从 **01-01 05:00**（含）起，
> `--until 2026-06-30` 到 **06-30 那一整天结束**（即 07-01 05:00 之前）。所以命令行筛出来的
> 和页面上看到的**是同一批消息**，凌晨那几条不会被两套口径分别算进去或漏掉。

> `命令/查看与导出/导出JSONL.cmd --session bili --since 2026-01-01 --mask` 这样带参数双击也能用。

---

### 13. 导出表情包（按使用次数排序）

双击 **`命令/查看与导出/导出表情包.cmd`**，把表情图导成一个文件夹：图片按**使用次数**编号命名，
外加一份 **`表情包清单.csv`**（带 BOM，Excel 直接打开不乱码）和一张 `index.html` 缩略图墙。

| 参数 | 作用 |
|---|---|
| `--session bili` | 只导某个会话（B站 1,177 个表情 / 微博 556 个） |
| `--used-only` | 只导**在聊天里实际用过**的（B站文本里命中 484 个 `[名称]`） |
| `--top 100` | 只要用得最多的 100 个 |
| `--with-sent` | 额外附上用到它的原话，方便看语感 |
| `--min-uses 2` | 过滤掉没人用的 |

---

### 14. 本地全文检索（不打开页面也能搜）

先把备份索引进本地数据库（SQLite FTS5，**全本地、不联网**）：
双击 **`命令/查看与导出/重建搜索索引.cmd`**（数据更新后重跑一次即可，10,837 条约几秒）。

然后双击 **`命令/查看与导出/搜索备份.cmd`**：直接进入**交互模式**，输入关键词回车，空行退出。

| 参数 | 作用 |
|---|---|
| `--session bili --sender peer` | 只看某个会话 / 只看对方说的 |
| `--kind count` | 按分类筛（`count` 真实发言 / `auto` 自动回复 / `sys` 系统通知） |
| `--since 2023-10-03 --until 2023-10-03` | 按「逻辑日」筛，一天 = **当天 05:00 → 次日 05:00**（与页面同口径） |
| `--context 2` | 每条命中多显示前后各 2 条 |
| `--regex "生日\|快乐"` | 正则匹配 |
| `--json` | 输出 JSON，方便接别的程序 |

> 索引里记着**建索引时的分界规则**（`meta.day_cut`）。如果索引是 05:00 分界之前建的，
> 搜索时会提示「索引的分界规则与当前不一致」并让你重跑一次 `命令/查看与导出/重建搜索索引.cmd` ——
> 那不是坏掉，是故意提示你刷新，免得命令行按 0 点分界、页面按 05:00 分界各说各话。

搜的范围和页面一致：正文 + 图片里的文字（OCR）+ 图片内容描述（VLM）。
中文用 **trigram 分词**，所以**查询词至少 3 个字**走索引；不足 3 个字会自动改用
字符串匹配（慢一点但结果一样）。

---

### 15. 隐藏某一条消息（右键菜单）

有些消息你不想让它出现在备份视图里。在**任意一条消息**上：

| 操作 | 桌面 | 手机 / 平板 |
|---|---|---|
| 打开菜单 | **右键**这条消息 | **长按 0.5 秒**（手指一移动就取消，不影响滚动） |
| 菜单项 | **🙈 隐藏这条消息** ／ **📋 复制文本** | 同上 |

**隐藏之后的显示行为**

| 位置 | 表现 |
|---|---|
| 浏览列表 | 完全不出现，不占位置 |
| 搜索结果 | 搜到它也不会冒出来，命中数如实减少（「命中 1 条」会变成「命中 0 条」） |
| 展开的上下文 | 只留一条**虚线占位条**「🙈 这条消息已隐藏　恢复」；**正文和图片都不进页面**，所以换任何关键词也带不出内容 |
| 侧栏 | 出现 **🙈 已隐藏 N 条** 按钮（一条都没隐藏时这个按钮不显示） |
| 统计 / 时间轴 / 导出 | **不受影响**。隐藏只改变"看不看"，不动 `data/messages.js`，也不影响按天互动统计和 JSONL 导出 |

**怎么恢复**

| 方式 | 操作 |
|---|---|
| 恢复这一条 | 展开它附近的上下文 → **点那条占位条**（或点它上面的「恢复」） |
| 恢复这一条（右键） | 右键占位条 → **↩ 恢复这条消息** |
| 恢复全部 | 侧栏 **🙈 已隐藏 N 条** → 点一下变成「⚠ 再点一次：全部恢复」→ 4 秒内**再点一次**才真的恢复（防误触；不点第二次就自动解除） |

> **记录存在哪**：浏览器的 `localStorage`，按数据源分开存（`dm-hidden-weibo` / `dm-hidden-bili`）。
> 所以**关掉页面再打开还记得**，**微博和 B站互不影响**。
> 清掉浏览器数据、或换台电脑打开，隐藏记录就没了 —— 但消息本身一直都在，不会丢。
>
> 注意：左栏 **✕ 清空全部条件** 只清搜索词与筛选条件，**不会**把你单独隐藏的消息放出来
> （那是"内容处置"，不是筛选条件）。

### 16. 图形界面（WebUI，不想碰命令行的走这条）

双击 **`命令/查看与导出/启动WebUI.cmd`**。会自动打开一个引导页面（`http://127.0.0.1:8787/`），
从「环境检查」到「导出」一共 7 步，按顺序点就行。

> ⚠️ 弹出的那个黑窗口**不要关** —— 关掉它等于关掉服务，网页就再也连不上了。
> 用完了回到那个窗口按 `Ctrl+C` 结束。
> 启动器会告诉你实际地址：**端口被占用时它会自己往后找**（8787 → 8788 → …），
> 所以别死记端口号，看窗口里那行「地址」。

| 步骤 | 干什么 | 说明 |
|---|---|---|
| 1 环境检查 | 看 Node / 浏览器 / OCR 环境 / 登录态 | 缺什么当场说清楚，不会跑到一半才炸 |
| 2 授权登录 | 开一个专用浏览器窗口，你自己在里面登录 | **不用把密码交给程序**（见下） |
| 3 选择会话 | 微博 / B站，或你加过的其它会话 | 也能填 / 改「要备份谁」的 uid |
| 4 选择范围并备份 | 增量还是全量、从哪天起，然后「开始备份」 | 日志实时滚出来，可以随时终止 |
| 5 加载他人备份 | 拖一个别人分享的 `.zip` 进来 | 解到 `imported/`，**只读** |
| 6 导出 | 选会话 / 时间范围 / 格式，先预览再导出 | 见下方「导出格式」 |
| 7 查看与维护 | 打开查看页 / 体检 / 看最近导出 | —— |

**几条要说明的：**

* **只在本机跑**：服务只监听 `127.0.0.1`，同一局域网里的其它设备访问不到。
  写操作还要求一个自定义请求头（浏览器跨站带不上），所以别的网页也没法偷偷指挥它。
* **登录不保存密码**：点「授权登录」只是帮你开一个干净的浏览器窗口，
  登录过程你自己在窗口里完成；程序通过浏览器的调试接口把 `Cookie` 读回来，存到本地
  （`data/cookie_header.txt` / `bili/cookie_header.txt`）。
  这两个文件**等同于账号密码**，别分享、别上传。
* **加载别人的备份**：第 6 步选「打包 zip」导出的文件发给别人，对方在第 5 步拖进去就能看
  （里面自带 `先读我.md`，写清了怎么用）。
  导入的内容一律标记为**只读** —— 抓取、OCR、压缩这些会写文件的操作对它全部拒绝，
  免得拿你自己的登录态去动别人的数据。不想要了，在第 5 步那一栏点删除即可。
* **勾多个平台会依次跑**：第 4 步同时勾上「微博 + B站」点开始备份，是**一个跑完自动接着跑下一个**
  （日志连着往下打）。为什么不并行：两个平台同时抓会抢同一个浏览器调试端口，
  后起的那个会直接失败。想中途停下，点「停止」会把**后面排队的也一起取消**。
* **体检发现索引里有「死键」可以自动修**：`命令/查看与导出/索引快照.cmd` 之后跑体检时，
  若报 `ocr.json 有 N 个键指向不存在的图片`（多半是图片压缩 webp→jpg 转码后旧扩展名的键没迁走），
  加 `--fix` 就能清掉：
  ```
  node scripts/doctor.mjs --fix --dry-run    # 先看会改什么
  node scripts/doctor.mjs --fix              # 真修（原文件会先备份成 ocr.json.bak-<时间戳>）
  ```
  图片文件本身不会被删，只清索引里那些指不到的键。
* **两种用法并存**：命令行的 `命令/备份与更新/更新备份.cmd` 那一套**完全不受影响**。
  两条路跑的是同一批脚本（WebUI 只是把它们串起来并显示日志），所以口径一致，
  可以放心混着用。
* 想了解它是怎么搭的 → **`WebUI说明.md`**。

**导出格式**（第 6 步）：

| 格式 | 适合什么场景 |
|---|---|
`jsonl` | 一行一条，喂给别的工具 / 脚本处理（默认）
`json` | 一个完整数组，方便直接看结构
`csv` | 用 Excel 打开（已处理逗号、换行、公式注入）
`md` | 纯文本归档，给人和 AI 读
`html` | 单文件网页，直接双击就能翻，不用装任何东西
**打包 zip（bundle）** | **分享给别人**用：内含消息 + 图片 + 索引 + 说明，对方拖进第 5 步即可浏览

---

## 二、目录结构

```
bili-weibo-dm-backup/
├─ 查看备份.html           查看页面（双击打开；顶部切换「微博 / B站」）
├─ 命令/                   ★ 全部双击启动器，按用途分 4 类
│  ├─ 备份与更新/
│  │  ├─ 更新备份.cmd        微博 · 一键增量更新（双击运行）
│  │  ├─ 重建备份.cmd        微博 · 全量重建（双击运行）
│  │  ├─ 更新B站备份.cmd     B站 · 一键增量更新（双击运行，首次需扫码登录）
│  │  └─ 重建B站备份.cmd     B站 · 全量重建（双击运行）
│  ├─ 查看与导出/
│  │  ├─ 启动WebUI.cmd       图形界面：本机引导式操作（见第 16 节）
│  │  ├─ 搜索备份.cmd        本地全文检索（双击＝交互式搜索，见第 14 节）
│  │  ├─ 重建搜索索引.cmd    重建本地检索索引（数据更新后跑一次）
│  │  ├─ 索引快照.cmd        索引快照与回滚（动索引前先留一份）
│  │  ├─ 导出JSONL.cmd       导出聊天记录为 JSONL（见第 12 节）
│  │  └─ 导出表情包.cmd      导出表情图 + 清单 + 缩略图墙（见第 13 节）
│  ├─ 图片与OCR/
│  │  ├─ 压缩图片.cmd        图片转 WebP 并删除原图（双击运行，会先问一次 Y/N）
│  │  ├─ 安装OCR环境.cmd     启用「搜图内文字 / 图片描述」（可选，双击运行）
│  │  └─ 卸载OCR环境.cmd     彻底卸载上述环境与索引（可选，双击运行）
│  └─ 配置与定时/
│     ├─ 生成会话清单.cmd    由 sessions.json 生成 sessions.js（见第 9 节）
│     ├─ 注册定时更新.cmd    注册自动更新（默认每周一 8:00，见第 11 节）
│     └─ 取消定时更新.cmd    取消上述计划任务
├─ 文档/                   补充说明（全部可选读）
│  ├─ 定时自动更新说明.md   定时任务：触发时间 / 来源 / 范围 / 存储 / 失败处理（见第 11 节）
│  ├─ WebUI说明.md          图形界面的架构 / 模块 / 数据存储 / 实现说明（见第 16 节）
│  ├─ WebUI安全审计.md      WebUI 的攻击面与已经加上的防线
│  ├─ 图片搜索方案.md       图片文字 / 内容搜索的实现与取舍
│  ├─ 备份与体检修复说明.md 多平台备份与体检孤儿键的修复记录
│  ├─ 登录校验-连不上接口-排查与修复.md  登录态判定的三分类与排查步骤
│  └─ 方案评估报告.html     功能评估报告 + 实施状态
├─ 使用说明.md             本文件
├─ sessions.json           会话清单（多会话的唯一数据源；改完跑「命令/配置与定时/生成会话清单.cmd」）
├─ sessions.js             ← 查看页读取的会话清单（由上面那个生成）
├─ scripts/
│  ├─ sessions.mjs         会话清单读取器（页面 / 更新脚本 / 索引脚本共用同一份）
│  ├─ build_sessions.mjs   由 sessions.json 生成 sessions.js
│  ├─ msg_kind.mjs         消息分类 + 打码口径（与查看页里的实现逐条对应）
│  ├─ update.mjs           微博主流程：取 Cookie → 拉取 → 规范整理 → 下图 → 出数据
│  ├─ sync_faces.mjs       同步微博官方表情图 → data/faces/
│  ├─ bili_update.mjs      B站主流程（接口、翻页、大整数处理都与微博版互相独立）
│  ├─ bili_cards.mjs       B站名片接口解析（头像 / 昵称）
│  ├─ bili_faces.mjs       同步 B站表情图 → bili/faces/
│  ├─ image_ocr.py         识别图片里的文字（`--set <会话key>` 切换数据集）
│  ├─ image_vlm.py         生成图片内容描述（`--set <会话key>` 切换数据集）
│  ├─ compress_images.py   图片转 WebP + 索引迁移 + 校验 + 删原图（五阶段）
│  ├─ export_jsonl.mjs     导出 JSONL（支持时间窗 / 打码 / 瘦身）
│  ├─ export_faces.mjs     导出表情包（按使用次数排序 + CSV + 缩略图墙）
│  ├─ build_fts.mjs        建本地全文检索索引（SQLite FTS5 / trigram）
│  ├─ search.mjs           本地全文检索（命令行 / 交互式）
│  ├─ snapshot.mjs         索引快照与回滚
│  ├─ doctor.mjs           一键体检（`--json` 可机读）
│  ├─ auto_update.ps1      定时更新的执行体（静默 + 日志轮转，UTF-8 带 BOM）
│  ├─ schedule_task.ps1    注册 / 取消 Windows 计划任务（UTF-8 带 BOM）
│  ├─ update.ps1           微博启动器：确保浏览器就绪再依次调用上面几个脚本（UTF-8 带 BOM）
│  ├─ bili.ps1             B站启动器：同上，改为打开 B站私信页、跑 B站版脚本（UTF-8 带 BOM）
│  ├─ server.mjs           本地 WebUI 服务端（只监听 127.0.0.1；零依赖）
│  ├─ run_pipeline.mjs     一条命令跑完整条链路（WebUI 的「开始备份」调它）
│  ├─ login.mjs            授权登录：开一个干净浏览器 → 只读 Cookie（不碰密码）
│  ├─ lib/                 服务端共用模块（路径 / 任务流 / zip / 导出引擎 / 导入）
│  ├─ cdp_lib.mjs          极简 CDP 客户端（零依赖）
│  └─ _explore/            逆向接口时用的探测/诊断脚本（保留备查，可删）
├─ data/                   微博数据集
│  ├─ messages.js          ← 查看页面读取的数据（自动生成）
│  ├─ messages.json        全量消息（同一份数据的 JSON 版）
│  ├─ faces.js             ← 表情名 → 表情图 的映射（自动生成）
│  ├─ faces/               微博官方表情图，约 620 张（自动同步，可删，下次更新会重新下）
│  ├─ ocr.js / ocr.json    ← 图片文字索引（自动生成）
│  ├─ vlm.js / vlm.json    ← 图片内容描述索引（自动生成，可选）
│  ├─ glm_key.txt          智谱 API Key（微博与 B站共用，本机私有，勿外传）
│  ├─ meta.json            统计信息（条数、时间范围、最后更新时间）
│  ├─ emoji_map.json       动图表情地址映射
│  ├─ cookie_header.txt    微博会话 Cookie（本机私有，勿外传）
│  ├─ _compression.json    图片压缩报告（候选数 / 改名映射表 / 实际删除数）
│  ├─ images/              压缩后的图片，174 MB（GIF 与头像按设计保留原格式）
│  └─ raw/                 过程产物与日志，可随时删除
│     └─ messages.bak.json 全量重建前的自动备份
├─ bili/                   B站数据集（结构与 data/ 一一对应）
│  ├─ messages.js          ← 查看页面读取的 B站数据（自动生成）
│  ├─ messages.json        全量消息（JSON 版）
│  ├─ faces.js             ← B站表情名 → 表情图 的映射（自动生成）
│  ├─ faces/               B站表情图（自动同步）
│  ├─ ocr.js / ocr.json    ← B站图片文字索引（自动生成）
│  ├─ vlm.js / vlm.json    ← B站图片内容描述索引（自动生成，可选）
│  ├─ meta.json            B站统计信息
│  ├─ emoji_map.json       B站表情地址映射（随消息接口一起返回）
│  ├─ cookie_header.txt    B站会话 Cookie（本机私有，勿外传）
│  ├─ images/              压缩后的图片，58 MB
│  └─ raw/                 过程产物：schema_audit.json（消息类型审计）、断点文件等
├─ search/                 本地全文检索库（可删，用「命令/查看与导出/重建搜索索引.cmd」重建）
├─ snapshots/              索引快照 zip（可删）
├─ exports/                导出产物：JSONL / 表情包 / 打包 zip（可删）
├─ webui/                  图形界面前端（index.html / style.css / app.js）
├─ imports/                拖进来待导入的压缩包（可删）
├─ imported/               已导入的他人备份，只读（可删）
├─ .webui/                 图形界面的运行状态：上次抓取 / 导出时间（可删）
├─ logs/                   定时更新的日志（可删）
├─ .edge-profile/          专用浏览器配置目录（两个网站的登录态都存这里，勿删）
└─ .ocr-env/               图片识别环境（约 330 MB，可选，用「命令/图片与OCR/卸载OCR环境.cmd」删）
```

---

## 三、技术说明

* **浏览器只做一件事：取 Cookie。**
  独立配置目录的 Edge 实例 + `--remote-debugging-port=9333`，通过 CDP 读取登录 Cookie
  （新版 Chromium 在默认配置目录下会忽略调试端口，所以必须用独立目录）。
  拿到 Cookie 后，所有接口请求和图片下载都由 Node 直接发起 —— 实测单页响应
  82～230 ms，且可重试；比在页面里 `fetch` 稳定得多（后者会被微博限流而卡死）。
* **数据接口**（微博网页版私信在用）：
  * 聊天记录：`GET /webim/2/direct_messages/conversation.json?uid=…&count=50&max_id=…`
  * 表情图：`GET /webim/pic_infos.json?pids=…`
  * 图片原图：`GET https://upload.api.weibo.com/2/mss/msget_thumbnail?fid=…&high=9999&width=9999&imageType=origin`
* **B站数据接口**（B站私信页 `message.bilibili.com` 在用，均已对照官方 API 文档核对）：
  * 登录态：`GET api.bilibili.com/x/web-interface/nav`（返回 `isLogin` / `mid` / `uname` / `face`）
  * 用户名片：`GET api.vc.bilibili.com/account/v1/user/cards?uids=<mid>`
  * 聊天记录：`GET api.vc.bilibili.com/svr_sync/v1/svr_sync/fetch_session_msgs?talker_id=…&size=500&end_seqno=…`
    （`size` 上限 2000；`end_seqno` 往更早翻页、`begin_seqno` 往更新翻页；
    响应里带 `has_more` / `min_seqno` / `max_seqno`，以及随消息返回的 `e_infos` 表情表）
* **增量策略**：从最新往回翻页，遇到「整页消息都已存在」就停止；每 50 页断点落盘，中断可续
  （B站版每 20 页落盘一次）。
* **同一个查看器装两套数据**：`查看备份.html` 里有一张 `SOURCES` 注册表 ——
  微博对应 `window.DM_DATA`，B站对应 `window.DM_DATA_B`（表情 / OCR / VLM 索引同样各自独立）。
  切换来源时把三栏 DOM 恢复成初始快照再重建，并摘掉上一轮的全局监听器，
  所以反复切换不会出现事件重复触发、徽章堆叠等问题（已用 5 轮切换压测验证）。
* **索引写入有两道保险**（图片文字识别 / 图片描述这类「跑很久、中途落盘」的任务）：
  ① **单实例锁** `data/vlm.lock`、`data/ocr.lock`（B站对应在 `bili/` 下）——
     同一数据集同时只允许一个写者，第二个实例会打印提示并以退出码 3 结束；
     锁里记的 PID 若已消失，视为陈旧锁自动清理。
  ② **落盘前与磁盘版本合并** —— 索引条目**只增不减**，某次运行拿着过期快照落盘
     也抹不掉别人已生成的结果。
  > 这两条是 2026-09-15 一次真实事故的产物：两个 `image_vlm.py` 实例并发，
  > 后落盘的那个把 174 条描述覆盖成 59 条。回归测试见 `scripts/_explore/verify_index_guard.py`。
* **免费模型降级链 + 输出清洗**（图片描述）：
  免费视觉模型高峰期会持续返回 1305「访问量过大」，单模型退避会整批卡死。
  脚本改为**多免费模型互备**（`glm-4.6v-flash → glm-4v-flash`，遇 429 立即换模型），
  并且**每张图的描述都先清洗再入库**（去 `<think>` / markdown 记号 / 小标题，
  关键词去重限量、总长限 200 字）。混用模型时条目里的 `m` 字段记录实际出结果的模型，
  `_meta.engine` 汇总可见。回归测试见 `scripts/_explore/verify_vlm_clean.py`。
* **隐私**：所有数据只存在本机 `data/`（微博）与 `bili/`（B站）目录，不上传任何地方。
  两个 `cookie_header.txt` 都是登录凭据，**不要把这些文件夹分享给他人**。
* **一条消息在磁盘上长什么样**（`data/messages.json` 里的一条；页面读的是同名 `.js` 包装）：
  `{ id, ts, time, from, type, text, recalled, images[{kind,fid,file,url,local}], card{kind,author,text,url,created} }`。
  `from` 只会是 `me` / `peer`；`type` 取 `text` / `image` / `link` / `card` / `video` / `emoji` / `voice`；
  撤回的消息**保留原文**并把 `recalled` 标为 `true`；
  `images[].local` 是相对本机路径，查看页据此直接加载图片。
  想把这些数据喂给别的工具，优先走**第 12 节的 JSONL 导出**（字段更全，还带 OCR 文字与图片描述）。

---

## 四、数据口径说明

| 情况 | 处理方式 |
|---|---|
| 普通图片 | 下载 `imageType=origin` 原图（最大 9999px） |
| 表情 / 贴纸（`media_type=9`） | 存为动图 `.gif`（`pic_infos` 返回的地址缺扩展名，脚本会自动补全后缀） |
| 动画表情（`media_type=15`） | 按原图存为 `.jpg`/`.gif`，并在气泡下方给出「打开动图」链接 |
| 视频 / 语音（`media_type=10` / `4`） | 视频与音频文件本身不备份，只保存**封面图**（取 `ext_text.video_pic_fid`） |
| 链接 | 把 `t.cn` 短链还原成真实长链后再展示 |
| 转发微博 / 卡片（`media_type=13` / `14`） | 保存作者、正文、原文链接，及卡片内图片 |
| 撤回的消息 | 接口返回 `recall_status=1` 时标记「已撤回」，但**原内容（文字/表情图）仍然保留并展示** |
| 文字表情 | `[委屈]`、`[泪]` 这类文字表情，以及 `/ee808d.png` 这类旧版大表情，都还原成微博官方表情图 |
| 分享的微博卡片 | 默认折叠；同一条微博的 `mid` 与短链会被识别为同一条，链接只留 1 个 |
| 动图表情消息 | 只显示表情图，不显示它自带的 `t.cn` 占位链接 |
| 图片去重 | 同一张图（相同 pid/fid）在多条消息里只存一份文件，多条消息共用 |
| 搜索的「上下文」 | 取自**完整会话**，不受搜索词 / 筛选条件影响；只用于查看，**不计入命中条数**；同一条消息不重复渲染 |
| 「一天」的边界 | **当天 05:00 → 次日 05:00**（00:00~05:00 的消息算**前一天**），不按自然日 0 点切。页面里所有按天的东西（日期分隔条、月份时间轴、跳到日期、日期区间筛选、折线图日轴、日历热力图、「一周里哪天最活跃」）以及命令行的 `--since` / `--until`，用的都是**同一条线** |

> 表情图的来源是微博官方接口 `GET /2/emotions.json?source=209678993&type=all`（564 个），
> 旧版大表情走 `img.t.sinajs.cn/t4/appstyle/expression/emimage/<名字>.png`。
> 原图多为 96×96 或 20×20，页面里统一按 `1.4em` 随文字缩放，所以不会显得突兀。
> 目前除 2 个 IP 联名表情（`[灵知]`、`[兔朱迪]`）外，99.8% 的表情都能还原。

---

## 五、常见问题

**Q：能备份其他人的私信吗？**
可以。改 `scripts/update.mjs` 顶部的 `PEER_UID` / `PEER_NAME` 即可。

**Q：上下文里怎么少了几条消息？**
两种可能，都是你主动设置的结果：
① 开着 **🚫 自动回复** —— 自动回复在上下文里被整条跳过；
② 那条消息被**右键隐藏**过 —— 会留一条虚线占位条「🙈 这条消息已隐藏」，点一下就能恢复。
另外左右两侧写的「更早 N 条 / 更晚 N 条」指的是**展开范围**，不是实际画出来的行数。

**Q：隐藏的某条消息找不回来了？**
先看左栏有没有 **🙈 已隐藏 N 条**：点它两次可以一次全部恢复。
只想恢复某一条的话，展开它附近任意一条消息的上下文，找到那条虚线占位条点一下即可。
如果左栏压根没有这个按钮，说明当前数据源没有隐藏记录（微博和 B站是分开记的）。

**Q：图片占了 2 GB，能小一点吗？**
**已经压好了**（见第 10 节）：微博 2.0 GB → 174 MB、B站 445 MB → 58 MB，原图已删。
以后新增的图想接着压，双击 `命令/图片与OCR/压缩图片.cmd` 就行（已压过的会跳过）。
想治本也可以把 `scripts/update.mjs` 里的 `MAX_IMG_DIM` 从 `9999` 改成 `2000` 之类，
让新图一开始就别下那么大（改完要跑一次「命令/备份与更新/重建备份.cmd」才对老图生效）。

**Q：为什么凌晨 3 点的消息算在前一天下面？**
因为「一天」是按 **05:00** 分界的（见第 2 节）：一条消息属于**它前面最近的那个 05:00** 那一天。
凌晨 3 点的消息往前最近的是**昨天** 05:00，所以它算昨天 —— 这样「23:40 聊到 01:20」的一整晚
对话会落在同一天，不会被 0 点劈成两半。消息气泡上的时间戳仍是**真实时间**（03:19 就是 03:19），
只有「归到哪一天」变了。如果你想改这个分界点，改 `查看备份.html` 里的 `DAY_CUT_H`（同时
`scripts/msg_kind.mjs` 的 `DAY_CUT_MS` 和搜索索引要一起重跑）。

**Q：浏览器窗口关了要不要紧？**
不要紧，下次更新会自动重新拉起。但**不要删** `.edge-profile` 目录，否则要重新登录。

**Q：B站那边一直提示「还没有 B站登录凭据」？**
说明专用浏览器里还没登录过 B站。双击 `命令/备份与更新/更新B站备份.cmd` 后，在弹出的窗口里
**扫码登录 B站**，登录成功后再双击一次即可。微博和 B站的登录态是分开存的，
其中一个登录了不代表另一个也登录了。

**Q：能同时更新微博和 B站吗？**
两个更新程序共用同一个专用浏览器实例，所以**先跑完一个再跑另一个**比较稳妥
（同时双击两下可能互相抢浏览器窗口）。先跑哪个都行。

**Q：微博和 B站的图片会混在一起吗？**
不会。微博图片在 `data/images/`，B站图片在 `bili/images/`，两边完全独立，不会互相覆盖。

**Q：提示「已有一个实例正在运行（PID xxxxx）」？**
说明同一个数据集已经有一个索引程序在跑（图片文字识别 / 图片描述）。
这是**故意拦住**的：两个实例各自拿着「启动那一刻的快照」，
后落盘的会把先落盘的成果覆盖掉（2026-09-15 真实踩过：174 条描述被压回 59 条）。
等它跑完再试就行。**确实确认它已经不在运行**（任务管理器里看不到对应 python），
可以删掉 `data/vlm.lock` 或 `data/ocr.lock` 再重试。

**Q：任务管理器里还留着 python 进程，要紧吗？**
要紧。中断更新时，**关掉命令行窗口不一定能杀掉它拉起的 python 进程**，
残留的那个会继续慢慢跑，并定期覆盖索引文件。
确认是残留的话，在任务管理器里按 PID 结束它就行，不会影响已经生成好的数据。
如果锁文件还指着那个已经消失的 PID，下次启动时会**自动识别为陈旧锁并清掉**，不用手动删。

**Q：想备份 B站上的另一个 UP 主？**
改项目根的 `sessions.json` 就行 —— `bili` 会话的 `peer.uid` 就是对方：
`peer.uid` = 对方空间地址里的那串数字，例如 `space.bilibili.com/1234567` → `1234567`；
`peer.name` 填对方昵称（留空也能跑，页面会显示「对方」）。
改完双击「命令/配置与定时/生成会话清单.cmd」。

**Q：B站的表情 `[tv_doge]` 没显示成图片？**
先跑一次 `命令/备份与更新/更新B站备份.cmd`（会同步表情图到 `bili/faces/`）。
另外 B站有一部分表情只在特定场景下才能拿到地址，所以可能有个别表情仍显示成文字 —— 
没找到图的表情记号会列在 `bili/raw/faces_missing.txt` 里，方便核对。

**Q：更新完页面没变化？**
按 `Ctrl+F5` 强制刷新一下（浏览器会缓存 `messages.js`、`faces.js`）。

**Q：表情显示不出来 / 显示成 `[委屈]` 这样的文字？**
说明 `data/faces/` 或 `data/faces.js` 缺失，双击一次 `命令/备份与更新/更新备份.cmd` 会自动补回来（不需要联网翻聊天记录，只补表情图）。

**Q：为什么「已撤回」那么多？**
其中 188 条是表情贴纸、1 条是红包，微博接口对它们都返回了 `recall_status=1`。
原始内容仍完整保留在页面上，不影响查阅。

**Q：统计图里对方那条线为什么几乎贴在底部？**
因为两方量级相差数十倍（我 6415 条 vs 对方 119 条）。面板**默认已开启「独立量程」**，
左右各一套刻度，两条线各自铺满图高；把它关掉就是同轴对比，对方会明显贴底 ——
那是真实情况，不是显示错误。另外「全部」范围有 959 天，图上每天不到 1.2 像素，
折线会很密；想看每天的细节，切到「近 90 天」（约 12 像素/天）最清楚。

**Q：统计数字和我用其它方式数的对不上？**
先看面板底部的「统计口径与数据来源」，那里列了排除项的具体条数与判定依据。
如果某类消息你认为不该算 / 该算，说一声改规则即可（改的是 `查看备份.html` 里的
`msgKind()` 与两条正则 `RE_GIFT` / `RE_SYSTIP`）。

**Q：图片里的文字能搜到吗？**
能，但需要先装一次识别环境（见上面第 7 节）。装完搜索框就能搜截图里的对话、
视频封面标题等。**不装也不影响其它任何功能。**

**Q：装完 OCR 环境占硬盘，怎么彻底清掉？**
双击 `命令/图片与OCR/卸载OCR环境.cmd`。它只删 `.ocr-env` 这一个文件夹（约 330 MB），
不碰别的地方；删完查看页照常用，而且**已识别的文字索引会保留，图片搜索依然有效**。
想连索引也清掉，卸载时对第二个问题选 `Y`。
（识别过程**不写用户目录**：不会产生 `~/.rapidocr`、AppData 缓存等残留，已实测确认。）

**Q：展开上下文后，「命中 N 条」的数字怎么没变？**
因为上下文是"顺带看一眼"，不属于搜索结果，所以不参与统计。它取自**完整会话**，
能看到被当前搜索词筛掉的消息 —— 这恰恰是它的用处。另外上下文**不会重复显示**同一条消息，
两条命中的上下文窗口重叠时也只会画一条分隔条。

**Q：展开上下文会不会很慢？**
不会。上下文直接取自本地的 `data/messages.js`，是**内存里的即时计算**，不联网、不查询，
点一下立刻出现。上下文越多，单次重绘的 DOM 也越多，但即便如此仍然是一次瞬时重绘。

**Q：OCR 会把我的私信图片传到网上吗？**
不会。**图内文字识别（OCR）**完全在本机跑，模型随程序包分发，**不联网、不上传**。
另外还有一个可选的**「图片内容描述」**通道：它只对 **OCR 一个字都没识别到的纯照片**
（当前 174 张风景 / 宠物 / 演出现场图）生效，会把这几张图发给智谱的免费视觉模型；
**所有截图（含文字的图）一张都不上传**，这条边界在 `scripts/image_vlm.py` 里是硬性的。
用完的模型只选**官方标注「永久免费」**的那几个，不产生费用。
不想用这个通道，不配 API Key 即可，其它功能完全不受影响。

**Q：有些图明明有字，却搜不到？**
常见三种情况：① 那类图还没被识别过 —— 跑一次 `命令/备份与更新/更新备份.cmd` 补齐；
② 字太小或太艺术（手写、特效字）识别不出来；③ 纯照片（风景、人像）本来就没文字。
另外 `[动画表情]`、贴纸这类表情图**故意不做识别**（没有搜索价值，省时间）。

**Q：压缩之后图片会不会糊了？**
默认 `quality=88` + 长边上限 2048，属于肉眼无损档；长截图（长宽比 > 2.5）上限会**自动放宽一倍**，
因为那类图基本是小字截图，压狠了字会糊。本来就没必要压的图（压完不变小）会**原样保留**。

**Q：为什么 `images/` 里还留着 GIF 和几个 jpg？**
按设计留的：**GIF 不动**（转 WebP 有丢帧风险）、**`avatar_*.jpg` 不动**
（更新脚本里头像路径是硬编码 `.jpg` 的）、还有 **21 张「压完反而更大」的图**也保留原图。
它们都不占什么体积。

**Q：压缩能把 2 GB 完整找回来吗？**
原图删了就回不来了（这正是省下 1.8 GB 的原因）。想反悔只能重新下载：
跑一次 `命令/备份与更新/重建备份.cmd` 会按接口重新拉一遍原图。所以**第一次跑建议先跑一次 `--dry-run` 看收益**
（`python scripts/compress_images.py --set all --dry-run`，不写盘、不删任何东西）。

**Q：删原图时提示「批量删除保护」/ 报错中断了？**
说明当前是在 **WorkBuddy 这类受控环境**里跑的（默认限制 50 个文件/轮）。
此时**索引已经迁移好、原图一张没删**，属于安全状态；
到**普通命令行窗口**里双击 `命令/图片与OCR/压缩图片.cmd` 重跑一次即可，已转码的会直接沿用。

**Q：定时更新没跑？**
先跑 `命令/配置与定时/注册定时更新.cmd -Status` 看任务在不在（默认是**每周一 8:00**，一周只跑一次）。
两个常见原因：① 任务按「**已登录时运行**」注册，注销/关机状态下不会跑；
② 时间还没到。错过的那次不用管，`StartWhenAvailable` 会在下次开机后补跑一遍。
想立刻验证一次：`命令/配置与定时/注册定时更新.cmd -RunNow`。跑完的日志在 `logs/` 目录下。

**Q：全文检索搜不到只有两个字的词？**
因为中文索引用的是 **trigram（3 字滑窗）**，不足 3 个字在索引层面匹配不到任何东西 ——
程序会自动改用字符串匹配兜底，结果一样，只是慢一点。所以**搜「生日快乐」走索引，搜「直播」走全表扫**。

**Q：导出 JSONL 时 `--mask` 打码够严吗？**
六类规则（手机号 / 邮箱 / 身份证 / 银行卡 / 账号 / 收货地址）都是**启发式**的，
规则命中数会如实报给你。地址这类非结构化内容只能尽力识别，导出前建议自己扫一眼。
正常文本不会被误伤（有回归断言守着）。

---

## 六、许可与使用限制

本项目的代码以 **[MIT 许可证](LICENSE)** 开源 —— 你可以自由使用、修改、分发，包括商用，
只要在副本里保留版权声明。完整条文见 [LICENSE](LICENSE)。

不过它处理的是**别人的隐私**，所以有几条使用边界请务必遵守，详见 **[使用限制.md](使用限制.md)**：

| | |
|---|---|
| ✅ **可以** | 备份**你自己账号**与某个人的私信 · 导出 / 检索 / 归档 · 打码后分享 · 改代码或商用 |
| ⛔ **不要** | 抓取别人的私信 · 用别人的账号登录 · 绕过平台权限 · 未经同意传播他人隐私 · 用于骚扰跟踪等违法用途 |

三句话记住：

1. **只备份你自己有权查看的会话**（你是对话的一方）。
2. **备份里装的是对方的隐私** —— 分享前先打码（导出时勾选），并征得对方同意。
3. **`data/`、`bili/`、`.edge-profile/` 永远不要整包发给别人**（前者是聊天内容，后者是登录凭据）。

> MIT 授予的是**法律层面的权利**，不会因为上面这些话而被撤销；
> [使用限制.md](使用限制.md) 说明的是**使用期待与风险提示**。
> 如果希望某些用途具备法律强制力，MIT 本身做不到 —— 详见该文档的最后一节。
