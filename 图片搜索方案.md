# 图片文字搜索 · 方案与落地记录

> 目标：让「搜索聊天记录」不再只能搜文字，也能搜到**图片里的内容**。
> 本文所有数字均来自本机 `data/images`、`data/messages.json` 与实跑结果，非估算。

---

## 一、结论速览

| 项目 | 结果 |
|---|---|
| 处理的图片 | **845 张**（全部处理完） |
| 识别到文字的 | **671 张（79.4%）**，共 **88,933 字** |
| 索引体积 | **253 KB**（`data/ocr.json` + `ocr.js`） |
| 安装体积 | **331.9 MB**（全部在 `.ocr-env` 一个文件夹内） |
| 实际花费 | **¥0**（本地识别，不调用任何付费接口） |
| 识别耗时 | 10 分 41 秒（845 张，单张中位 695 ms） |
| 隐私 | **图片不出本机**，不联网、不上传 |

搜索框现在可以直接搜到截图里的对话、视频封面标题、设定图标注 ——
命中的图片会描边亮起，并在下方显示「图内文字：…」并高亮关键词。

---

## 二、图片家底（实测）

消息 7,921 条，含图消息 1,896 条，**去重图片 1,016 张**，本地 1,018 个文件，原图共 **2,021 MB**。

| 前缀 | 数量 | 内容 | 处理 |
|---|---|---|---|
| `photo_` | 729 | 私信直发图：**截图、照片、设定图** | ✅ 识别（589 张有文字） |
| `card_` | 100 | 分享微博时带的配图 | ✅ 识别（68 张有文字） |
| `vcover_` | 16 | 视频封面（常带标题文字） | ✅ 识别（14 张有文字） |
| `pic_` | 156 | 正文是 `[动画表情]` 的动图表情 | ❌ 跳过（无搜索价值） |
| `emoji_` + `avatar_` | 17 | 表情包、双方头像 | ❌ 跳过 |

**实际可识别目标 = 845 张**，跳过的 173 个文件都是表情/头像。

### 关键障碍：原图是超高清巨图

`photo_` 长边普遍 **7,499 × 9,999**（最高约 1 亿像素，PIL 会触发 `DecompressionBombWarning`）：

* 任何云端 OCR / 视觉 API 都会直接拒收（普遍限 4,000–6,000 px、4–5 MB）
* 识别前必须缩放，否则又慢又糊

实测缩放策略（长边 1600 / JPEG q88）：845 张**全部达标**，压缩后平均约 191 KB。

---

## 三、技术选型：为什么最终用 RapidOCR 而不是 PaddleOCR

| 对比项 | PaddleOCR 原版 | **RapidOCR（最终采用）** |
|---|---|---|
| 依赖框架 | 庞大的 PaddlePaddle C++ 框架 | 仅 ONNXRuntime |
| 安装体积 | **约 2 GB+** | **331.9 MB** ✅ |
| 模型来源 | 运行时从服务器下载 `.pdmodel` | **模型随包分发，零下载** ✅ |
| 是否写用户目录 | 会写 `~/.paddleocr` 缓存 | **完全不写** ✅ |
| Windows CPU 稳定性 | 有已知 `PIR+OneDNN` 报错 | 成熟稳定 |

考虑到「硬盘紧张 + 要能卸载干净」，**装机体积小 6 倍、运行时不下载、不写用户目录**这三点是决定性的。
精度方面 RapidOCR 用的就是同一套 PP-OCR 模型（本项目为 **PP-OCRv6**），中文识别效果一致。

### 一个实测得出的优化

原本担心竖版长截图整体缩放后小字会糊，做了「纵向分段识别」。实测同一张 6088×9999 的微博截图：

| 方式 | 结果 |
|---|---|
| 不分段 | 11 行，置信 **0.991**，911 ms，`vts` 识别正确 ✅ |
| 分段 6 块 | 12 行，置信 0.989，2419 ms，重叠区重复 + 出现误读 `yts` ❌ |

**结论：普通竖版截图整体缩放反而更准**，于是把分段阈值从「高/宽 > 1.35」放宽到 **2.5**，
只对真正的超长图才分段 —— 又快又准。

---

## 四、成本：全流程 ¥0

| 路径 | 829→845 张一次性 | 说明 |
|---|---|---|
| **本地 RapidOCR（已采用）** | **¥0** | 无任何 API 调用 |
| 云端图片理解（可选，未启用） | ¥0 | 智谱 GLM-4.6V-Flash 官方永久免费；需注册 key |

**对比当初调研的云端方案**（若不用本地而全走云端）：

| 方案 | 845 张一次性 | 备注 |
|---|---|---|
| 智谱 GLM-4.6V-Flash | ¥0 | 永久免费，单并发约 1 req/s |
| 百度 OCR 标准版 | ¥0 | 免费额度 1000 次/月内；全自费也仅 ¥4.2 |
| Qwen3-VL-Flash | ¥0 → ¥0.15 | 免费 100 万 token / 90 天 |
| 腾讯云 OCR | ¥0 → ¥126 | 0.15 元/次，**贵约 30 倍，不推荐** |

> 无论走哪条路，本次任务都是 **0 元**；最坏情况（全量付费）也不过几块钱。

---

## 五、可卸载保证（已实测验证）

用户提出的前提是「能保证卸载干净」，因此这一步做了完整实测。

### 写入位置清单 —— 只有两处

| 路径 | 内容 | 体积 |
|---|---|---|
| `.ocr-env/` | Python 环境 + PP-OCRv6 模型 | 331.9 MB / 8,444 个文件 |
| `data/ocr.json` + `data/ocr.js` | 文字索引（纯数据） | 253 KB + 253 KB |

### 卸载实测结果

| 检查项 | 结果 |
|---|---|
| 运行环境是否彻底删除 | ✅ 331.9 MB / 8,444 文件 → **目录不存在** |
| 是否误伤其它文件 | ✅ `.edge-profile`(725 MB)、`data/images`(2,021 MB)、`data/faces`、`scripts` 等 9 项逐一比对**完全一致** |
| 用户目录残留 | ✅ **零写入** —— 无 `~/.rapidocr`、无 `AppData/Local/RapidOCR`、无 `.paddleocr` |
| pip 缓存 | ✅ 本次用 `--no-cache-dir`，未产生任何缓存（现有 10.4 MB 缓存是 18 天前的旧物） |

### 卸载后功能依然可用（关键设计）

索引是**纯数据**，与运行环境解耦。因此在**环境已彻底删除**的状态下重新跑验证：

**19 / 19 项全部通过** —— 图片搜索照常命中、统计照常显示、页面无任何报错。

也就是说：**卸载 331.9 MB 的运行环境后，253 KB 的索引让「搜图片文字」功能继续有效**，
只是不能再识别以后新增的图片。若连索引也想清掉，卸载时选 `Y` 即可。

---

## 六、交付物

| 文件 | 说明 |
|---|---|
| `安装OCR环境.cmd` | 一键安装（约 330 MB），装完可自动开始识别 |
| `卸载OCR环境.cmd` | 一键卸载，只删 `.ocr-env`，可选是否连索引一起清 |
| `scripts/image_ocr.py` | 识别脚本，支持断点续跑 / 增量 / 只跑指定文件 |
| `data/ocr.json`、`data/ocr.js` | 文字索引（自动生成） |
| `查看备份.html` | 已集成：搜索框可搜图内文字，命中时描边 + 显示片段 + 高亮 |
| `更新备份.cmd` | 已集成第 4 步：自动识别新增图片（未装环境则自动跳过） |

---

## 七、尚可选的增强（未做）

剩下 **174 张纯照片**（风景、人像等）没有文字可识别，目前搜不到。
若要「搜到画面内容」（例如搜「猫」「舞台」能找到对应照片），需要图片理解模型：

* **智谱 GLM-4.6V-Flash**（免费）：需注册 API key，把 174 张压缩图上传识别，约 5 分钟
* 这是原方案里的「分层路由」后半段，因为需要 API key 与图片上传（隐私权衡），
  **默认未启用**。需要的话再说一声即可补上。
